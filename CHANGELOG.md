# CHANGELOG

## 2026-09-15 — dsh-mcp-background：错峰启动队列

### 背景
DSH 启动瞬间会并发 spawn 全部 7 个 MCP 子进程（Node / Python / uvx），CPU 与磁盘 I/O 瞬时打满，导致桌面端首屏卡顿、启动缓慢。

### 改动（`dsh-mcp-background/index.js`）
- **首屏让行**：启动后前 1.5s 不启动任何 MCP，把资源让给界面渲染
- **错峰串行队列**：MCP 逐个启动，前一个连上（或失败）后立即放行下一个，快的不用等
- **单服务 4s 熔断**：某个 MCP 卡住超过 4s 自动让出队列槽位，转后台重试，不拖累后续服务
- **手动绿色通道**：界面上的连接/重连操作绕过队列，即时生效

### 效果
- 界面秒开；MCP 在后台静默逐个就绪
- 不再出现启动瞬间的 I/O 风暴

---

## 2026-09-08 — 初始整理
- 建立 dsh-plugins 仓库，作为 `profiles/plugins` 的唯一真源
- 统一各插件 README 的上游归属与本地改动说明
