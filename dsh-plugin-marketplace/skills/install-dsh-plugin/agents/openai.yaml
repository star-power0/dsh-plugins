interface:
  display_name: "Install DSH Plugin"
  short_description: "Safely install or update verified DSH plugins"
  default_prompt: "Use $install-dsh-plugin to safely install or update this Registry-verified DSH plugin."
